# Arkalia CIA - Requirements Python
# Backend pour l'application mobile Flutter

# Base de données
# Note: sqlite3 est inclus dans Python standard

# Traitement de fichiers
pypdf==6.11.0  # Security updates (GHSA advisories fixed)
python-magic==0.4.27
# pathlib2==2.3.7  # Removed: yanked version, using built-in pathlib

# APIs et web
fastapi==0.136.1  # Updated: Compatible with starlette 0.49.1 (security fixes)
uvicorn==0.47.0
pydantic==2.9.2  # Downgraded for safety compatibility
starlette==0.49.1  # Updated: Security fix (CVE-2025-62727)
requests==2.34.0

# Sécurité et validation
cryptography==48.0.0
python-multipart==0.0.28
slowapi==0.1.9  # Rate limiting pour FastAPI
authlib==1.7.2  # Security updates (GHSA advisories fixed)
passlib[bcrypt]==1.7.4  # Hashing de mots de passe
PyJWT==2.12.1  # JWT tokens
python-jose[cryptography]==3.5.0  # Alternative JWT (compatibilité)
bleach==6.3.0  # Sanitization HTML/XSS protection
phonenumbers==9.0.30  # Validation téléphone internationale

# Utilitaires
python-dotenv==1.2.2
pydantic-settings==2.14.1  # Configuration avec Pydantic Settings
click==8.1.8  # Downgraded for semgrep compatibility
rich==15.0.0  # Downgraded for semgrep compatibility

# Tests
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
httpx==0.27.0
reportlab==4.5.1  # Génération PDFs de test

# Logging
loguru==0.7.3

# Machine Learning
prophet==1.1.5
numpy==1.26.4  # Gardé à 1.26.4 pour compatibilité avec mobile-base-sdk
pandas==2.2.2
